import React from 'react';

/* eslint-disable-next-line */
export interface FieldsProps {}

export const Fields = (props: FieldsProps) => {
  return (
    <div>
      <h1>Welcome to fields component!</h1>
    </div>
  );
};

export default Fields;
